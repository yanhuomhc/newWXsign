<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yanhuo.sign.dal.mapper.ext.UserExtMapper" >
    <resultMap id="BaseResultMap" type="com.yanhuo.sign.dal.model.User" >
        <id column="u_id" property="uId" jdbcType="BIGINT" />
        <result column="u_name" property="uName" jdbcType="VARCHAR" />
        <result column="u_pwd" property="uPwd" jdbcType="VARCHAR" />
        <result column="u_sex" property="uSex" jdbcType="BIT" />
        <result column="open_id" property="openId" jdbcType="VARCHAR" />
        <result column="u_institute" property="uInstitute" jdbcType="VARCHAR" />
        <result column="u_power" property="uPower" jdbcType="INTEGER" />
        <result column="u_email" property="uEmail" jdbcType="VARCHAR" />
        <result column="s_no" property="sNo" jdbcType="VARCHAR" />
    </resultMap>

    <select id="selectByName" resultType="com.yanhuo.sign.dal.model.User">
        SELECT u_name
        FROM USER
        WHERE u_name = #{uName}
    </select>

    <select id="login" resultMap="BaseResultMap"></select>

    <select id="selectByOpenId" resultMap="BaseResultMap">
        SELECT *
        FROM USER
        WHERE open_id = #{openId}
    </select>



    <select id="login" resultMap="BaseResultMap">
        SELECT *
        FROM USER
        WHERE u_name = #{uName} AND u_pwd = #{uPwd}
    </select>

    <select id="selectAllTeachers" resultMap="BaseResultMap">
        SELECT u_id,u_name,u_sex,u_institute,u_power,u_email
        FROM USER
    </select>

    <select id="selectTeacher" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT u_id, u_name, u_sex, u_institute, u_power, u_email
        FROM USER
        WHERE u_id = #{uId}
    </select>

    <update id="updateTeacherInfo" parameterType="com.yanhuo.sign.dal.model.User" >
        UPDATE USER
        SET
        u_pwd = #{uPwd},
        u_name = #{uName,jdbcType=VARCHAR},
        u_sex = #{uSex,jdbcType=BIT},
        u_institute = #{uInstitute,jdbcType=VARCHAR},
        u_power = #{uPower,jdbcType=INTEGER},
        u_email = #{uEmail,jdbcType=VARCHAR}
        WHERE u_id = #{uId}
    </update>

</mapper>